name: day40
on: workflow_dispatch
jobs:
  envtasks:
    runs-on: ubuntu_latest
    env:
      APP_NAME: myapp
      ENVIRONMENT: staging
      VERSION: 1.0.0
    steps:
      - name: Use environment variables at 3 levels
        env:
          APP_NAME: myapp
          ENVIRONMENT: staging
          VERSION: 1.0.0
        run: |
          echo "Use of environment variables:"
          echo "App name: $APP_NAME"
          echo "Environment: $ENVIRONMENT"
          echo "Version: $VERSION"
