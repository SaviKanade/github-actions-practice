name: hello workflow

on:
  push: 
    branches: [main]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v4
        run: echo "Hello from GitHub Actions!"


      
