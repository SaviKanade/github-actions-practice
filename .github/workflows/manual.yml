name: manual-work

on: 
  workflow_dispatch: 
    inputs:
      environment: 
        description: These are the enviornment list. Select one
        required: true
        type: choice
        options: 
          - production
          - staging
          

jobs:
  manual-task:
    runs-on: ubuntu-latest
    steps:
      - name: Environment name
        run: echo ${{ inputs.environment }}
