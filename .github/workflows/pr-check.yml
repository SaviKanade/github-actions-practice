name: task-day-41

on:
  pull_request_target:
    types: [opened, synchronize] 

jobs:
  PR-check:
    runs-on: ubuntu-latest
    steps:
      - name: PR check running for branch
        run: $ {{ github.head_ref }}
